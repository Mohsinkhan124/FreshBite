export const metadata = { title: "Manage Coupons" };

/**
 * Scaffold placeholder — not yet built. Coupon Management wasn't part
 * of the features built so far (Products/Categories/Orders Management
 * were); this is left as a placeholder rather than a fake implementation.
 */
export default function AdminCouponsPage() {
  return (
    <section className="container-fb py-16">
      <h1 className="text-3xl font-semibold">Manage Coupons</h1>
      <p className="mt-3 text-ink-500">Coupon management isn&apos;t built yet.</p>
    </section>
  );
}
