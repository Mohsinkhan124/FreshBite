export const metadata = { title: "Analytics" };

/**
 * Scaffold placeholder — not yet built. The Admin Dashboard home
 * (/admin) already covers monthly sales, order status, and top
 * products; a dedicated deeper analytics page wasn't part of the
 * features built so far.
 */
export default function AdminAnalyticsPage() {
  return (
    <section className="container-fb py-16">
      <h1 className="text-3xl font-semibold">Analytics</h1>
      <p className="mt-3 text-ink-500">
        Deeper analytics and reporting aren&apos;t built yet — see the Dashboard for current charts.
      </p>
    </section>
  );
}
